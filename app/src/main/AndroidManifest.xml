<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rubatto.staralba">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NoticeActivity" />
        <activity android:name=".ManageAdActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".MypageActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".RegisterAdActivity" />
        <activity android:name=".GuideAdActivity" />
        <activity android:name=".ModifyActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".BusinessWebviewActivity"></activity>
    </application>

</manifest>